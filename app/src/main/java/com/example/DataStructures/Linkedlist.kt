package com.example.DataStructures

import android.util.Log

/*class Linkedlist<T>{

    var head:Node<T>?=null
    fun addNode(value: Int)
    {
        if (head==null)
        {
            head= Node()
            head!!.value=value
        }
        else
        {
            if (head!!.next==null)
            {
                head!!.next= Node()
                head!!.next!!.value=value
            }else{
                head!!.next!!.next=Node()
                head!!.next!!.next!!.value=value
            }

        }
    }

    fun printNodes()
    {
        println(head!!.value)
        println(head!!.next!!.value)
        println(head!!.next!!.next!!.value)
    }
}
class Node<T>{
   var next:Node<Int>?=null
    var value:Int?=null
}

fun findB(bString:String):Int?{
    var requiredLength =0

    var i =0
    while (i+1<bString.length){

            if (bString[i].equals('(') && bString[i+1].equals(')')){
                i+=2
            }else{
                requiredLength+=1
                println("indexValue --"+i+"  value--"+requiredLength)
                i++
               // Log.d("ith index--","coming")
            }


   }
    return requiredLength
}*/




fun main()
{
/*    val linkedlist = Linkedlist<Int>()
    linkedlist.addNode(4)
    linkedlist.addNode(6)
    linkedlist.addNode(56)
    linkedlist.printNodes()
    val value = findB(")(()(")
    println("value -----"+value.toString())*/
}